apiVersion: 1

providers:
  - name: DataPulse
    orgId: 1
    folder: DataPulse
    type: file
    disableDeletion: false
    editable: true
    options:
      # Relative to GF_PATHS_HOME (monitoring/grafana), not the yaml file directory.
      path: conf/provisioning/dashboards
